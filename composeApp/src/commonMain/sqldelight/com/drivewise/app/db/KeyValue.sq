CREATE TABLE KeyValue (
  key TEXT NOT NULL PRIMARY KEY,
  value TEXT NOT NULL
);

selectByKey:
SELECT value FROM KeyValue WHERE key = ?;

insert:
INSERT OR IGNORE INTO KeyValue(key, value)
VALUES (?, ?);

update:
UPDATE KeyValue
SET value = ?
WHERE key = ?;

deleteByKey:
DELETE FROM KeyValue WHERE key = ?;
